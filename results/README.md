# Results

Simulation outputs belong under scenario-specific directories in this tree.
Results are never source inputs. The existing `experiments/` table is retained
only as provenance for a historical transformer-parameter experiment; it is
not yet a canonical 2022 validation reference.
