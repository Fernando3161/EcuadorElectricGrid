# Configuration

Runtime and scenario configuration belongs here. The existing files predate
the v1 runtime boundary and remain for review: `config.yaml` contains broad
PyPSA-Earth extraction settings, and `environment_ec.yaml` is a legacy Conda
definition. Phase 2 will establish `requirements.txt`; Phase 4 will establish
the authoritative scenario schema.
