# Scripts

Explicit command-line and preprocessing entry points live here. Scripts must
use the production package in `src/ecuador_grid`, repository-relative paths,
and documented inputs and outputs.

`preprocessing/` currently contains the two Python demand-preparation scripts
that previously lived beside notebooks. They preserve existing behavior and
are not yet the supported v1 baseline workflow.
